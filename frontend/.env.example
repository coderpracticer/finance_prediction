# Leave empty when using Vite dev proxy on the same server.
VITE_API_BASE_URL=

# Or set an explicit backend URL when serving frontend/backend separately.
# VITE_API_BASE_URL=http://<server-ip>:8000

